{
  "name": "example",
  "version": "1.0.0",
  "description": "Example serverless website",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "local": "clear && serverless invoke local --function app",
    "deploy": "clear && serverless deploy"
  },
  "author": "charlie smith",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "serve-static": "^1.15.0",
    "serverless-http": "^3.1.0"
  },
  "devDependencies": {
    "serverless-apigw-binary": "^0.4.4",
    "serverless-aws-latest-layer-version": "^0.4.2",
    "serverless-aws-static-file-handler": "^4.0.1",
    "serverless-offline": "^11.2.1"
  }
}
